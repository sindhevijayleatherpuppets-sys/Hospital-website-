# Database connection URL (Default SQLite for local dev/testing)
DATABASE_URL="file:./dev.db"

# JWT Secret for session authentication (Generate a 32+ character random secret in production)
JWT_SECRET="replace-with-a-secure-random-32-byte-secret-key"
